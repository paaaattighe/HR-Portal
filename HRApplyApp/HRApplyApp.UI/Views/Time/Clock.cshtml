@model HrApplyApp.Models.Employee

@{
    ViewBag.Title = "Clock";
}

<h2>Hello @Model.FirstName</h2>

<span id=tick2>
</span>

<script>

/*By JavaScript Kit
http://javascriptkit.com
Credit MUST stay intact for use
*/

    function show2() {
        if (!document.all && !document.getElementById)
        return thelement = document.getElementById ? document.getElementById("tick2") : document.all.tick2;
        var Digital = new Date();
        var hours = Digital.getHours();
        var minutes = Digital.getMinutes();
        var seconds = Digital.getSeconds();
        var dn = "PM";
        if (hours < 12)
            dn = "AM";
        if (hours > 12)
            hours = hours - 12;
        if (hours == 0)
            hours = 12;
        if (minutes <= 9)
            minutes = "0" + minutes;
        if (seconds <= 9)
            seconds = "0" + seconds;
        var ctime = hours + ":" + minutes + ":" + seconds + " " + dn;
        thelement.innerHTML = "<b style='font-size:20;color:blue;font-family:Verdana;'>" + ctime + "</b>";
        setTimeout("show2()", 1000);
    }

    window.onload = show2;

</script>
<br/>

@using (Html.BeginForm("ClockIn", "Time", Model, FormMethod.Post))
{
    @Html.Hidden("id", Model.EmpID)
    <button style="width: 15%;" class="btn btn-primary">Clock In</button>
}
<br/>
@using (Html.BeginForm("ClockOut", "Time", Model, FormMethod.Post))
{
    @Html.Hidden("id", Model.EmpID)
    <button style="width: 15%;" class="btn btn-primary">Clock Out</button>
}


@section Admin {
    <div class="navbar-collapse collapse">
        <ul class="nav navbar-nav navbar-right">
            @Html.ActionLink("HR Apply App", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })

            <li class="dropdown-toggle" id="dropdownMenu1" data-toggle="dropdown">
                <a href="#">Policies<span class="caret"></span></a>
            </li>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("View Policies", "Index", "Policies")</li>
                <li>@Html.ActionLink("Manage Policies", "Manage", "Policies")</li>
                <li>@Html.ActionLink("Manage Categories", "ManageCategory", "Policies")</li>
            </ul>

            <li class="dropdown-toggle" id="dropdownMenu2" data-toggle="dropdown">
                    <a href="#">Time Tracking<span class="caret"></span></a>
                </li>
                <ul class="dropdown-menu">
                    <li>@Html.ActionLink("Submit Time", "Index", "Time")</li>
                    <li>@Html.ActionLink("View Timesheet", "Index", "Time")</li>
                </ul>
            <li>@Html.ActionLink("Admin", "Admin", "Home", new { area = "" }, null)</li>
        </ul>
    </div>

}